# Migrations

SQL migrations live here and are the source of truth for PostgreSQL schema changes.

Do not use ORM schema sync. Create reviewed SQL migrations before adding
repositories, database-facing services, feature services, or controllers that
depend on new tables.

No tables have been created yet.
